global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules/alerts.yml

scrape_configs:
  - job_name: campus-errand
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app:8081"]
